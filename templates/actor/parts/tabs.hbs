<nav class="sheet-tabs tabs" data-group="primary">
    {{#each tabs.primary.tabs as |tab|}}
    <a class="item {{#if tab.active}}active{{/if}}" data-action="tab" data-group="primary" data-tab="{{tab.id}}">
        {{#if tab.icon}}<i class="{{tab.icon}}"></i>{{/if}}
        {{tab.label}}
    </a>
    {{/each}}
</nav>
